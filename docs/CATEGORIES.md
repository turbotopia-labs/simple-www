# Categories

Categories are plain text values in Markdown front matter.

Suggested defaults:

- `general`
- `demo`
- `notes`
- `files`
- `catalog`
- `system`

Keep category names short and stable so they can be used for filtering later.
