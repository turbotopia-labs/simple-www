# Modules

Modules are configured in `data/config.json`.

Each module has:

- `label`: Display name.
- `enabled`: Whether it appears on the site.

Initial modules:

- `news`
- `projects`
- `blog`
- `downloads`
- `store`
- `admin`
